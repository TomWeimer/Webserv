server {
	listen 4242;
	root ../front-end;
}

server {
	listen 4243;
	root ../front-end;
}

server {
	listen 4244;
	root ../front-end;
}

server {
	listen 4245;
	root ../front-end;
}

server {
	listen 4246;
	root ../front-end;
}

